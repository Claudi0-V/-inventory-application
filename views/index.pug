extends layout

block content
  h1= My Store
  p Categories:

  if items
    each item in items
      .item
      a
        h2= item.name
        h3= item.description
  else
    h2= "there's no items here"

